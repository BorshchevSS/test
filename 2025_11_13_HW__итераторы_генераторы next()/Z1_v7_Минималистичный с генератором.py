def recipe_generator(instructions):
    """
    Генератор, который yields шаги рецепта
    """
    for step_number, instruction in enumerate(instructions, 1):
        yield step_number, instruction


def run_recipe_generator(instructions):
    """
    Реализация с использованием генератора и next()
    """
    print("Рецепт начинается! Следуйте инструкциям.\n")

    # Создаем генератор
    recipe_gen = recipe_generator(instructions)

    try:
        while True:
            # Получаем следующий шаг из генератора
            step_number, instruction = next(recipe_gen)

            # Показываем шаг
            input(f"Шаг {step_number}: {instruction}\n(Нажмите Enter для продолжения...)")

    except StopIteration:
        print("\n🎉 Поздравляем! Вы завершили рецепт!")

# Список шагов рецепта
recipe_instructions = [
    "Подготовить все ингредиенты.",
    "Нарезать овощи кубиками.",
    "Обжарить лук до золотистого цвета.",
    "Добавить морковь и тушить 5 минут.",
    "Залить водой и довести до кипения.",
    "Посолить и поперчить по вкусу.",
    "Подавать горячим."
]

# Запускаем с генератором
run_recipe_generator(recipe_instructions)